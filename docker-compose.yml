services:
  app:
    build: .
    container_name: news-rss
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./logs:/app/logs
    # ports:
    #   - "3000:3000"
    networks:
      - news-rss-network

networks:
  news-rss-network:
    driver: bridge 